# Gemini CLI Adapter - google-drive-mcp

> **Generated adapter.** Shared property behavior lives in [INSTRUCTIONS.md](INSTRUCTIONS.md) and [property-context.yaml](property-context.yaml). Do not edit this file directly.

## Read First

1. [INSTRUCTIONS.md](INSTRUCTIONS.md)
2. [STATE.md](STATE.md)
3. [PLAN.md](PLAN.md)

## Adapter Notes

- This file exists for GEMINI.md discovery only. It is not the source of truth.
- No hook system should be assumed.
- Read the local INSTRUCTIONS.md before any non-trivial work.
- Follow the neutral property contract even when the CLI idioms differ.
- On session start, run `node "$SYNTAX_HOME/scripts/session-start.mjs" --root=<property-root>` if your runtime did not already surface an equivalent SessionStart hook (also resolves to ../../syntax/scripts/session-start.mjs).
- On session close, prefer `"$SYNTAX_HOME/scripts/session-close.mjs"` (if `$SYNTAX_HOME` is unset, the `syntax/` checkout sibling to `qntx/` - `../../syntax/scripts/session-close.mjs`); declare claim checks with the required `--claim-check=verified:<evidence>|none:<reason>` flag, task state with `--clickup=logged:<ids>|none:<reason>`, plus `--corrections-reviewed` and `--test-scope`. Otherwise follow [../../syntax/commands/session-close.md](../../syntax/commands/session-close.md).
- If you close by hand instead of running the runner (constrained shell, no `$SYNTAX_HOME` access), run the shared validator as the gate before declaring close complete: `node "$SYNTAX_HOME/scripts/verify-session-close.mjs" --root=<property-root>` (also resolves to `../../syntax/scripts/verify-session-close.mjs`; exit 1 = close is not valid). It is the same check the runner invokes — the fail-closed backstop so a hand-written close still can't skip STATE/PLAN/ledger/memory enforcement.
